// Area of a rectangle
#include <stdio.h>

int main() {
        int L;
        int W;
        int Area;
        printf("Enter The Length : ");
        scanf("%d",&L );
        
        printf("Enter The Width : ");
        scanf("%d",&W);
        Area = L * W;
        printf("Area is: %d",Area);
        
        
    return 0;
}